apiVersion: n9/v1alpha
kind: Project
metadata:
  name: {{ .Name }}
  displayName: {{ .Name }}
  labels:
    origin:
      - sloctl-e2e-tests
  annotations:
    team: green
spec:
  description: Dummy Project for sloctl docker image e2e tests
